{
  "name": "webster-router",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "playground": "bun run --watch playground/index.ts",
    "build": "bun run build.ts",
    "prepublishOnly": "bun run build"
  },
  "dependencies": {
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-plugin-dts": "^0.2.1",
    "bun-types": "^1.0.18"
  },
  "module": "src/index.js",
  "files": [
    "dist"
  ]
}
